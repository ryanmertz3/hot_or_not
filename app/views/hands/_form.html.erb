<%= form_with(model: hand) do |form| %>
  <% if hand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hand.errors.count, "error") %> prohibited this hand from being saved:</h2>

      <ul>
        <% hand.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :count %>
    <%= form.number_field :count %>
  </div>

  <div class="field">
    <%= form.label :table_id %>
    <%= form.number_field :table_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
